(function(){

	if(document.getElementById('metro-user-input')) {
		postMetroInput()			
	}

})()

function postMetroInput() {
	/*
	need to add the rest of the XHR call here
	*/

	// var form = document.querySelector('form')
	// var data = new FormData(form)
	// var req = new XMLHttpRequest()
	// req.send(data)
}